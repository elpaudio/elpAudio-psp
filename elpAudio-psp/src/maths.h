#pragma once
#include "main.h"
#include <math.h>

extern double lengthdir_x(double len, int dir);
extern double lengthdir_y(double len, int dir);

extern double clamp(double val, double min, double max);
